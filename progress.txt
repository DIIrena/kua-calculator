SCAFFOLD-001 - 2026-05-13 - Flask app scaffolded as a single-file module rather than a package factory because the calculator has exactly three routes and no need for blueprints. Decision: link Google Fonts (Fraunces + Spectral) once in base.html with preconnect, rather than self-hosting; speed matters less for this top-of-funnel tool than visual distinctiveness, and the design skill is explicit that defaulting to Inter/Roboto loses character. Set frame-ancestors '*' on /embed at the route level (response header), keeping CSP scoped to that one route so site-wide security headers can still be added later without breaking the iframe use case.

METHODOLOGY-001 - 2026-05-13 - Chapter 6 copied verbatim via cp; chose python-markdown's 'extra' + 'toc' + 'sane_lists' as the rendering extension stack. Key learning: the 'extra' bundle silently includes md_in_html, which is exactly what lets the inline-SVG figures from chapter 6 render without escaping. Heading IDs from the toc extension follow a predictable slug pattern ('5. The Kua number' becomes '5-the-kua-number'), which means SEO-001 deep links are reliable as long as section headings in methodology.md never change wording. Worth noting: scroll-margin-top on h2 and h3 prevents the sticky-header-style cropping that would otherwise hide deep-linked headings under any future fixed-position site bar.

KUA-LOGIC-001 - 2026-05-13 - Built calculateKua as a pure function with the digital-root reduction and the Kua 5 reassignment baked in. The non-obvious correctness moment: when a male calculation hits subtractFrom - reduced = 0 (which can happen post-2000 if reduced = 9), the result must wrap to 9, not be left at 0. The +9 wrap is in place. Also confirmed the female reassignment fires when reduceToSingleDigit(reduced + addend) lands on 5, not when (reduced + addend) is literally 5: female 1990 reduces last-two-digits 90 to 9, then 9+5=14 reduces to 5, then reassigns to 8. The chapter 6 phrasing 'if the result is 5' was ambiguous about whether reassignment happens before or after the second reduction; the worked examples settle it (after).

DIRECTIONS-001 - 2026-05-13 - Built the Kua-to-direction-quality mapping for all 8 valid Kuas. Honored chapter 6's worked examples for Kua 4 and 9 verbatim, and used the cultural-commons Eight Mansions table for the remaining six. The key finding while writing this: chapter 6 says East-group Kuas have favourable in N/S/E/SE and West-group in NE/SW/W/NW, which is a hard constraint - it means a correctness check on the table is just "does every Kua N's favourable codes land in the right four directions?" That structural check catches transcription errors better than spot-checking individual cells. Plain-English meanings were written with the marketing copywriting skill's specificity rule (each meaning names a concrete USE, e.g. 'point your bed's headboard this way', rather than abstract description). Honest-framing audited: no 'will', no 'guarantees', no outcome promises. Used 'Traditionally' as a hedge on Jue Ming's harshest line.

UI-001 - 2026-05-13 - The biggest task; landed in three files (index.html template, ui.js, main.css). Decisions worth recording: (1) The radio buttons are styled label-as-button pads with the native input visually hidden but kept in the DOM for accessibility, then revealed via :has(input:checked) for the active state. This pattern gives a 48px touch target instead of the ~16px native radio circle and looks calm with the editorial aesthetic. (2) The result card uses a <template> element cloned at render time, with [data-bind="..."] attributes acting as the slot interface. This keeps the markup declarative and means SEO-001 can later edit copy inside the template without touching JS. (3) The reduced-motion media query disables the cardRise animation and the smooth scroll. (4) The result card has a 4px gradient bar across the top (terracotta to sage) - a single editorial touch that signals "this is a finished thing" without adding noise. The frontend-design skill's instruction to commit to one memorable detail rather than scatter micro-interactions justified that call.

COUPLES-001 - 2026-05-13 - Disclosure pattern: standard real <button> with aria-expanded + aria-controls, no JS for the keyboard handling because the native button element gives Enter/Space for free. The shared-rooms data structure (both / avoid / mixed) reads each compass direction once and bins it - O(8) and trivially correct. Honest framing in 'mixed': 'Assign these rooms by primary user' rather than declaring one person's preference wins, which would be the wrong call in a tool meant for two people to read together. Shared-rooms section is inserted BEFORE the methodology strip so the conversion CTA stays last.

PRINT-001 - 2026-05-13 - Print rules wrapped in @media print despite the link element already using media='print' - belt and braces, and lets the file be loaded unconditionally in the future without breaking screen view. The key accessibility consideration: favourable vs unfavourable must be readable in greyscale, since most home printers print B&W by default. Solved by three redundant cues (the ★ vs × icon, the solid vs dashed left border, and the 'Favourable' / 'Avoid' text), so colourblind readers and B&W printers both work. Used mm units for print dimensions (consistent with A4 conventions) and pt for type (consistent with print typography).

SEO-001 - 2026-05-13 - The marketing copywriting skill and the ai-seo skill both pushed in the same direction: lead with the direct answer, keep answer blocks to 40-60 words, match heading wording to query phrasing. Took that literally for the FAQ: each acceptedAnswer text in the FAQPage schema is in that snippet-extraction sweet spot. The hardest call: should the 'Is this fortune-telling?' FAQ be the first item or buried? Chose to keep it as the last item rather than leading with the skepticism. Reason: search visitors land asking 'what is my Kua number?' - they want the answer first, the honesty pass second. Burying the skepticism would be deceptive, but front-loading it would make the page read defensively. Last-position lets the page answer the practical question first while still naming the limit in the same scroll.

EMBED-001 - 2026-05-13 - Made the calculator partial (_calculator.html) the single source of truth for the form + result-card markup, so embed.html and index.html share one definition. Embed page is intentionally NOT a base.html descendant - it skips the header/footer/nav entirely, which is what an iframe-embed view needs. Added robots: noindex to the embed page so it does not compete with the main / for the same keyword. The CSP header (frame-ancestors '*') was already set in SCAFFOLD-001 via the route handler; nothing to add here. Result card's methodology links inside the embed open target='_blank' so they don't navigate the iframe out of the parent page.

CNY-001 - 2026-05-13 - User flagged a correctness concern: the original form took only birth year, relying on a helper hint that told users to manually subtract one year if born before Chinese New Year. That's what chapter 6 actually documents, and it matches most online Kua calculators, but it's high-friction and error-prone. Upgraded the form to year + month + day, added a CNY-date lookup table for 1900-2050 in cny.js, and added effectiveKuaYear(year, month, day) which returns the adjusted year and a metadata object describing whether an adjustment happened. The result card now shows a sage-tinted notice when an adjustment fires ('Your birth date is before Chinese New Year (Feb 9, 1986), so the Kua formula uses 1985 as your Chinese solar-calendar year, not 1986'). Verified the worked examples still work: male Jul 1978 -> Kua 4, female Aug 1985 -> Kua 9. Verified the new edge case: female Jan 15, 1986 (before CNY Feb 9, 1986) -> kuaYear=1985 -> Kua 9. The change also closes a real correctness gap because users who didn't read the helper text were silently getting the wrong Kua. Added a new FAQ entry covering CNY handling, both in the visible page and the FAQPage JSON-LD.

CONTENT-002 - 2026-05-21 - Rewrote the methodology page (Phase 2). The chapter-6-verbatim hard rule was overridden for kua-calculator by the 2026-05-21 architect spec, so this page now diverges from the feng-shui dashboard's chapter 6 on purpose. The biggest editorial lever was not reorganising sections but splitting compound sentences into single-idea sentences, which made the logical chain explicit without adding words. The original prose often had one sentence doing two jobs and leaned on transitions the reader had to infer. Section 8 'Limitations and honesty' was already the strongest section; the work there was removing qualifier phrases that softened claims the text should own directly. Section heading wording was kept close to the originals so the auto-generated slug IDs stay stable for the result-card deep links.

BRAND-BOOK-001 - 2026-05-21 - Created the brand book at brand/BRAND_BOOK.md ahead of the rebrand. The non-obvious correctness moment: the user asked for 'bold orange-forward with white text on orange', but white on #ff6200 measures only 3.0:1 contrast, which fails WCAG AA for body text. Near-black #1a1a1a on #ff6200 measures 5.8:1 and passes, and black-on-vivid-orange is actually the bolder, more memorable look anyway, so the brand book mandates dark text on orange panels and reserves white for the logo and large graphics only. Also swapped the display font from Space Grotesk to Bricolage Grotesque after reading the frontend-design skill, which explicitly names Space Grotesk as an overused converged AI choice to avoid. The brand book ends with a ready-to-paste CSS :root token block so REBRAND-001 implements it without re-deriving values.

REBRAND-001 - 2026-05-21 - Rebranded all views by rewriting main.css around the brand-book tokens and updating the four templates plus print.css. The cleanest move was a full main.css rewrite rather than a patch: a 1172-line stylesheet built on parchment and Fraunces has those concepts threaded through every rule, so find-and-replace would have left orange values sitting in parchment-shaped layouts. Keeping every class selector identical meant zero JavaScript changes were needed; ui.js still emits the same class names and data attributes, so the result card and shared-rooms section restyle for free. The one template restructure was index.html: the form was nested inside the hero, so an orange hero with the form on top would have put every white card on orange. Splitting the hero into an orange text band plus a separate calculator-section, then pulling the form card up with a negative top margin, gives the layered look while keeping the form on a calm grey surface. The logo went in as inline SVG with fill=currentColor so a single color property recolors it for light or dark contexts.

REBRAND-QA-001 - 2026-05-21 - QA pass on the rebrand. The useful lesson: static verification caught what a screenshot could not. Headless screenshots confirmed the hero, form, methodology, and embed render correctly, but the result card only appears after a JS form submission that a screenshot tool cannot trigger. Rather than build an interaction harness, the check that actually mattered was confirming via git that all four JS files are byte-identical to the Phase 1 build, and confirming every class name ui.js generates is styled in the new main.css. That proves the result card renders correctly without needing to drive it. Also verified zero em dashes across all 10 changed files, balanced CSS braces, the four semantic landmarks, and that the methodology deep-link anchors survived the content rewrite.

BRAND-NATURAL - 2026-05-22 - The brand was reworked twice after Phase 2: first a bold orange-forward rebrand, then a full pivot to the current natural brand once the user shared mood boards. The lasting lesson: the mood boards were a far better brief than any verbal instruction - they pointed unambiguously to quiet naturals, soft texture, and restraint, the opposite of the bold blocks built first. Synthesising them into a concept ('unity and attraction of opposites' - yin and yang) and getting that approved before rebuilding stopped a long thrash of one-off colour tweaks. Final brand: warm cream and sand canvas, deep olive green #4f5a36 carrying the whole header lockup as a single colour (mark, wordmark, and script signature), clay accent, deep ink footer as the grounding counterweight, Hanken Grotesk throughout with Bilbo Swash Caps reserved for the 'My Feng Shui Home' signature, a soft radial light wash rather than a literal repeating texture, and a botanical bamboo-sprig motif. Fully accessible, no contrast override. brand/BRAND_BOOK.md v2 is the source of truth; the prd.json REBRAND notes describe the superseded orange phase.

NEXT-SCAFFOLD-001 / CALC-MIGRATE-001 / PRIVACY-CLEANUP-001 - 2026-05-22 - Phase 3 Stage 1 code build. Migrated the thin Flask app to Next.js 16 (App Router, TypeScript, Turbopack) on the phase-3-nextjs branch. The key decision that made this low-risk: the calculation was already 100% client-side vanilla JS, so the four scripts were copied byte-for-byte into public/calculator/ and reused unchanged - no rewrite of ui.js. The markup of _calculator.html became a static JSX component (CalculatorIsland.tsx) with every element id preserved, since ui.js selects on those ids. Two deviations from the plan, both forced by Next.js 16: the middleware convention was renamed to proxy.ts (middleware is deprecated in 16), and a (site) route group was needed because nested layouts in Next wrap rather than replace - so /embed lives outside the group to render chrome-free while every other page gets the SiteHeader/SiteFooter. npm run build and npm run lint pass clean; all 12 routes generate. Flask fully removed.

SUPABASE-SCHEMA-001 / AUTH-001 / ACCOUNT-001 - 2026-05-22 - The Supabase-backed account layer is fully coded: @supabase/ssr clients, the proxy session refresh, the sign-in page (magic link + Google), the auth callback and sign-out routes, the auth-gated account dashboard with save-details / opt-in / delete-account server actions, and supabase/migrations/0001_init.sql (profiles + saved_charts, RLS, the handle_new_user trigger). These tasks are left passes:false on purpose: their acceptance criteria need a live Supabase project, which is the owner's external setup. The build runs fine with placeholder env vars because the public pages do not touch Supabase; only sign-in/account need real keys, and they currently show a clear 'account not connected yet' notice. CLAUDE.md hard rules were amended to record that the account layer deliberately uses Supabase and stores account-holder data, while the calculator core and /embed stay privacy-pure.

DEPLOY-001 - 2026-05-22 - Phase 3 Stage 1 shipped. The site is live at https://myfengshuihome.com (the owner's first production deploy). Vercel deploys from GitHub main; the custom domain was connected via Hostinger DNS. Two lessons from the DNS step worth keeping: first, the apex A record needed not just the right value but the removal of leftover Hostinger AAAA (IPv6) records - the domain kept resolving to Hostinger over IPv6 until those were deleted. Second, Vercel defaulted to www as the primary domain and the apex redirected to it; setting the apex as primary in Vercel made myfengshuihome.com (no www) the canonical URL, which matches the brand and the NEXT_PUBLIC_SITE_URL env var. A real bug was caught by the dev server before deploy: the two calculator <template> elements, ported as JSX children, caused a React hydration mismatch that left template.content empty and would have broken the result card; rendering them via dangerouslySetInnerHTML fixed it (setting innerHTML on a <template> populates .content, and React does not hydrate the children). Stage 1 is complete: free calculator, magic-link accounts, email capture, all live. Deferred follow-ups: Google OAuth provider config, and branded auth email via Resend custom SMTP. Next: Stage 2, the 2-color chart.

NEXTAUTH-MIGRATION - 2026-05-24 - Replaced Supabase Auth with Auth.js v5 (NextAuth) running on the Next.js server so the auth flow lives on our own domain: the Google consent screen now reads "Sign in to myfengshuihome.com" and the magic-link email is delivered from hello@myfengshuihome.com via Resend, with the brand palette inlined. Supabase stays as the database only; the @auth/supabase-adapter owns the four adapter tables in a new next_auth schema, and public.profiles and public.saved_charts were recreated with FKs to next_auth.users (RLS disabled because access control now happens in Next.js server code through the service-role client). Four real gotchas were discovered the hard way and are worth remembering. First, the canonical adapter migration grants permissions on the next_auth schema but does NOT expose it via the Supabase REST API; the adapter calls failed with AdapterError on getUserByEmail until next_auth was added to Project Settings -> API -> Exposed schemas (the schema existed in Postgres but PostgREST refused to serve it). Second, Next.js 16 forbids cookie mutation in Server Components; the /account page was reading and deleting a marketing-opt-in cookie during render and crashed with "Cookies can only be modified in a Server Action or Route Handler" - the fix was moving the cookie consumption into NextAuth's events.signIn callback, which runs inside the auth Route Handler where cookies are mutable. Third, NextAuth blocks linking an OAuth provider to a pre-existing user from a different provider by default (OAuthAccountNotLinked error); since the same user had first signed in via magic link, Google sign-in failed until allowDangerousEmailAccountLinking: true was set on the Google provider - this is safe because both providers verify email ownership, and the "dangerous" name is a warning to think about it rather than a real risk in this case. Fourth, signOut with redirectTo: "/" felt broken because / itself redirects to /kua-calculator and the visitor read it as a missing page; redirecting straight to /sign-in is the right pattern for a sign-out button. End state: own-domain branded auth shipped live, with both magic-link and Google working through the same /api/auth/callback/* on myfengshuihome.com. The two Stage 1 deferred follow-ups (Google OAuth provider config and branded auth email) are both done as part of this migration.

STAGE-2-CHART - 2026-05-24 - Stage 2 of the staged roadmap shipped: signed-in users can save a Kua chart and view it on a dedicated chart page rendering both a bagua diagram (inline SVG octagon) and the eight-row color-coded direction table. Three useful learnings to keep. First, the calculation now exists in two places on purpose: the original vanilla JS in public/calculator/ stays unchanged because the embed and the privacy-pure anonymous calculator must remain dependency-free, and a TypeScript port at lib/kua.ts + lib/directions.ts + lib/cny.ts lives on the server so saveChart can recompute server-side and be the source of truth for what is persisted (a malicious client cannot persist a chart that does not match its inputs). Second, the bagua SVG's contrast trap: paper text on the brand --clay (#be6b43) measures ~3.85:1 - it fails AA Body for the 14-16px labels because they are below the 14pt-bold AA Large threshold. Switched the avoid panels to --clay-deep (#9c5331) which gives 5.33:1 AA Body. The lighter --clay still appears on the chart page as a left-border accent on direction-table avoid rows, where border colour has no contrast threshold and the dashed style is a redundant greyscale cue. Third, Server Components in Next.js 16 cannot attach onClick handlers, so the Print button was extracted into a tiny ChartPrintButton client island instead of relying on an inline window.print() script - cleaner code, same UX. Saved charts store a frozen snapshot of the result jsonb at save time, which means future copy edits to direction meanings will not retroactively rewrite already-saved charts (that is the correct behaviour for a saved artefact). Print stylesheet folded into globals.css @media print rather than a separate /public/chart-print.css so it ships through the same Next.js CSS pipeline as the rest of the styles; brand colours forced on bagua fills so colour printing keeps the visual coding, with icon + dashed-border redundancy guaranteeing greyscale fallback.

CHART-POLISH-AND-DASHBOARD - 2026-05-24 - Same-day iteration on Stage 2 that the owner drove through several rounds of visual feedback. /account became the working dashboard: brand mark below the heading, the full calculator embedded so the owner can run charts for other people without leaving the page, and a Compare two charts form that opens a new /account/compare/[a]/[b] view rendering both bagua diagrams with the shared-rooms grouping (favourable-for-both, avoid-for-both, mixed). Then the chart view itself was reorganised to a sketch the owner sent: 4 favourable cards in a single row above the bagua, 4 avoid cards in a row below, brand header at the very top, footer URL at the bottom. The biggest design lesson was that bold panel colours felt wrong for a calm brand - swapping the bagua fills from solid olive / clay-deep to pale --green-soft #d8debf and --clay-soft #f0d5c0 (both AAA with ink text) made the whole page read as the "natural and bright" the brand book asks for. The biggest engineering lesson was that the PDF one-page constraint is brittle under flex layouts: an explicit chart-page height with overflow:hidden, flex-grow on the diagram, and margin-top:auto on the footer all looked correct in code but the print engine kept pushing the footer URL onto a second page. The winning approach was the boring one: natural document flow, the WHY paragraph hidden in print so the row heights are deterministic, and a fixed 140mm bagua. Total content sits comfortably under one A4 and the footer falls naturally on page 1. The dedicated north-arrow group was removed because the ring labels around the octagon already include "North" - two markers competing in the same spot read as a mess. The chart view page also moved to max-width 1080px and a forced 4-column card grid (with 2 / 1 breakpoints) so the on-screen layout matches the PDF.

STAGE-3-EMAIL - 2026-05-24 - Stage 3 of the staged roadmap built: the chart email. Saving a chart with marketing_opt_in true now fires exactly one branded HTML email to the account holder via Resend, and the chart view page has an "Email me this chart" button that sends the same email on demand, rate-limited to 5 per user per day via a new public.email_sends table. The email reuses the same Resend wiring as the Auth.js sign-in email (no Supabase Edge Function, contradicting the 2026-05-22 spec but matching the 2026-05-24 spec's revised plan), through a tiny lib/resend.ts helper. lib/email-chart.ts builds both the HTML and the plain-text versions; the HTML is an inline-styled table layout (the only layout primitive Outlook renders reliably) and intentionally omits the SVG bagua because most email clients strip SVG - instead the CTA links to /account/chart/[id] where the bagua renders properly. Rate limiting uses an UPSERT of (user_id, day) -> count and is enforced both for the on-demand button and the auto-send path. The auto-send is wrapped in try/catch in save-chart.ts so a Resend failure or rate-limit hit never breaks the chart save itself; the on-demand path surfaces failures to the user via ?email=error query state on the chart page. The owner needs to apply 0003_email_sends.sql in the Supabase SQL Editor before the email button works in production; until then the auto-send fails silently and the manual button shows the error pill. Resend stays bounded by the existing AUTH_RESEND_KEY (no new env var).

STAGE-3-POLISH-AND-BAGUA-PNG - 2026-05-25 - Same-day Stage 3 iteration the owner drove. Added a /account/email-sent confirmation page so the "Send my chart by email" button on /account has somewhere clear to land; sendChartEmailInternal grew a status enum (sent / rate-limit / send-failed / threw) so the redirect param could reflect the actual outcome. Moved the chart-view email pill to the top of the page (it was rendering at the bottom alongside the action buttons, but the post-submit redirect put the visitor at the top of the page so the pill was invisible). Daily cap raised 5 -> 9. The hardest part was the bagua PNG. First attempt: inline SVG in the HTML email - Gmail strips <svg> elements for security so the chart was invisible. Second attempt: rasterise SVG to PNG server-side via /api/chart-image/[id] using @resvg/resvg-js - works in principle, but Vercel's serverless Linux has no system fonts so Resvg renders the bagua segments correctly and then silently skips every <text> element. Fix: ship real TTF files (HankenGrotesk Regular / Bold / ExtraBold, ~200KB) in lib/fonts/ and tell Resvg to load them via fontFiles + loadSystemFonts: false. Initial attempt loaded WOFF from @fontsource and that didn't work - resvg-js says it supports WOFF but reality differs. Real TTF files in the project tree are bundled by Next file tracing automatically and just work. Final wrinkle: the first deploy with the font fix still showed no text because Gmail's image proxy had cached the previous text-less PNG against the same chart URL (Cache-Control was immutable, max-age=1 year). Added a ?v=3 cache-buster to the email's <img src> and relaxed the Cache-Control on the route to max-age=3600, must-revalidate. Lesson: if your renderer might evolve, do not ship images with immutable max-age=1y.

STAGE-4-HOMEPAGE - 2026-05-25 - Stage 4 of the staged roadmap shipped: the My Feng Shui Home homepage at /, an articles system, a 14-point checklist lead magnet, an interactive room picker, and the sales page for the first paid product (the $29 Home Harmony Map). The page-IA + tier model + sample copy came out of a project-architect mini-pass that drew on the sibling projects/feng-shui/ library (22 audited chapters, the author's own 18-idea monetisation list, and the Drinčić Institute price list as evidence the audience pays). Homepage moved from app/page.tsx (which only redirected) into app/(site)/page.tsx so it gets the SiteHeader / SiteFooter chrome; the old root page.tsx was deleted to avoid a routing conflict. SiteHeader rework: brand link goes to /, brand-text reduced to the Bilbo Swash Caps signature alone (one-line "My Feng Shui Home"), Articles added to nav, Kua Calculator demoted from brand-text to nav item. Voice rules: short sentences (one idea each), bold the shift word, italic for emphasis, no em dashes, no outcome promises, Flesch grade 6-7. Two practical engineering choices saved a day each. First, the lead-magnet "checklist" is served as a printable /checklist Server Component (the user prints to PDF from the browser) instead of a binary PDF attached to the email - zero new dependency, zero binary in the repo, and the page itself has a print button. Second, the article system uses TypeScript metadata (lib/articles.ts) with markdown bodies (content/articles/*.md) instead of front-matter inside the .md files - typed slugs, no YAML parser dep, easy to grep for the gated flag. Three seed articles ship: myths (free, 4 of 12 myths from chapter 11), five-elements (free, plain-English vocabulary from chapter 2), diagnostic-walkthrough (gated, the full 7-step practitioner method from chapter 21 that the homepage teaser links into when signed in). The Home Harmony Map sales page is a Stage 4 stub: the $29 price, the FAB-framework "what you get" / "what it is not" copy, and an early-list email capture that reuses the sendChecklist action so the visitor gets the checklist delivered immediately (real value, list captured, no separate plumbing). Stripe + personalised PDF generation are explicitly Stage 5.

STAGE-5A-KICKOFF - 2026-05-26 - Stage 5a started: the modular product engine + the first paid product, the Personal Feng Shui Compass ($14, ~20-page PDF). The big design pivot from the architect spec the owner originally heard was that the personalisation runs on three inputs (birth date, gender, first name) - not on rooms or floor plans. Personalisation comes from the customer's Kua and the East/West group split, both already calculated by lib/kua.ts + lib/directions.ts (Stage 2). The product line will scale to eight SKUs (single life pillar $7, single space $7, year ahead $9, personal compass $14, pick-three bundles $17, all-pillars / all-spaces $29, flagship $49, couple compatibility $19) all running on the same engine; flagship ships last because it bundles everything. Three concrete deliverables in this turn: supabase/migrations/0004_product_orders.sql (three new tables for orders, responses, deliveries; owner runs the SQL + creates the product-pdfs Storage bucket); the engine scaffold (lib/products.ts is the catalogue, lib/blocks.ts is the markdown-block loader with a small mustache substitution layer for {{firstName}}, {{kuaNumber}}, {{direction}} etc); and the first content block, content/blocks/identity-{east,west}.md, two variants at ~2200 words each. Voice combines SKILL_WRITING_VOICE_HP (sensory anchors, rhythm variation, scale similes) with marketing/copywriting (second-person address, benefit-led, clarity); honest framing every line; zero em dashes. Identity block is the owner-approval gate per the spec - no other content block gets written until the owner reads it and signs off. Next stop: that review. After approval, four good-direction blocks (Sheng Qi, Tian Yi, Yan Nian, Fu Wei) and four bad-direction blocks spawn as parallel writer subagents. After Stripe keys arrive from the owner, MAP-STRIPE-001 unblocks. Lesson worth banking: writing prose in the HP voice with second-person address (rather than third-person past) means borrowing the prose-level techniques (sensory specificity, varied rhythm, scale comparisons, the trick of dropping an absurd specific into an otherwise serious paragraph) without the POV apparatus. The result reads warm and storytelling without becoming a children's novel.
PROD-004 - 2026-06-13 - Bedroom Reset built (23pp PDF). Learning: a heading-promotion bug in the build pipeline initially tripled page count to 41; the all-9-Kua static format removes per-customer rendering entirely, making delivery identical to the planner's proven path.
PROD-005 - 2026-06-13 - Money Kit built (30pp). Learning: pure typography tuning plateaued under the page target; a generated contents page + chapter openers landed the band honestly without padding copy.
PROD-006 - 2026-06-13 - Diagnostic Workbook built (28pp). Learning: print worksheets need nowrap spans around checkbox+label pairs or Chrome strands boxes from their labels; injected page breaks beat shrinking type when a note card orphans.
PROD-007 - 2026-06-13 - Ritual Pack built (13pp). Learning: orphaned section headings are best fixed by promoting them to divider pages with an index, not by squeezing; card grids with dashed cut guides print to clean quarter-sheets.
PROD-008 - 2026-06-13 - Cures Catalogue built (23pp). Learning: a 7-column quick-table fits portrait Letter at 7.6pt fixed layout; the checker correctly forced crystals to Traditional/Preference labels only, never Tested.
PROD-011..014 - 2026-06-13 - Wave 3A four static products built in-thread (no subagents; session limit was on the workflow runner, not the main loop). Learning: a generic build_product_pdf.py + product-generic.html (cover/title/subtitle/framing passed as CLI args, preamble cards preserved, all tables -> worksheet class, label regex handling both **Tested** and **Tested.**) builds any draft uniformly - cleaner than per-product scripts. Drafts survived the session-limit crash (it killed the check/build stage, not the writing). Adversarial sweep found the drafts already clean: BaZi handled fortune-telling correctly, Healthy Home made no medical claims, Five Elements cycles correct; only fix was one "stated plainly" -> "stated outright". Page counts 34/26/15/24, all in band.
PROD-010/009/015 - 2026-06-13 - Wave 3 remainder: the three deferred products built in-thread, content fanned out to voice-checking workflows. Learning 1 (the latent bug): the Compass personalized path had never executed in production because it was launched:false, so two faults sat undiscovered - buildHtml hardcoded "Compass" on the PDF cover, and the success form button + delivery email said "Compass". Adding a SECOND personalized product forced the generalization (product.coverTitleHtml; generic form/email copy), which also unblocks the Compass itself. Lesson: a feature that has never run live is unverified no matter how clean it looks; the first real consumer of a shared path is where its hardcoded assumptions surface. Learning 2 (form dispatch): the post-checkout success page assumed one form shape for all personalized products; the Move-In Report needs a date window, so I added a personalizedForm "kua" | "movein" discriminator on the commerce entry and branched the page + fulfilment action on it - cleaner than a per-product page. Learning 3 (data-as-bundle): the Move-In report imports the 243-day calendar JSON statically (not via runtime readFile) so Next bundles it into the function automatically; the success route also got content/blocks added to outputFileTracingIncludes so the personalized fulfilment action can read blocks on Vercel. Learning 4 (the third fulfilment type): the 7-Day Reset introduced "course" alongside static/personalized - the webhook enrols + sends the welcome, a daily Vercel cron sends days 1-7 with a 20h spacing gate (so a double-firing cron cannot double-send) and advances day_sent only after a successful send, and unsubscribe is stateless (HMAC of email:course keyed with AUTH_SECRET, no stored token). Learning 5 (workflow content + main-thread code): drafting the 7 Extended blocks and the 8 drip emails as parallel draft -> adversarial-voice-check pipelines caught real leaks the drafters missed ("chapter"/"book" structural mentions, soft outcome-promise framing, a doubled task under one heading) while the code wiring stayed in the main thread to keep shared files (commerce.ts, the success page) coherent. All three build clean; smoke tests pass (19 blocks resolve with zero token leaks; window filter correct; unsubscribe token round-trips). All three ship launched:false - owner creates each Stripe Price + env var (STRIPE_PRICE_EXTENDED / _MOVEIN / _RESET), and for the course also CRON_SECRET + migration 0008, then they flip live.
VISUAL-COMPLETION - 2026-06-13 - Autonomous pass while the owner was out: "finish all the products you can." Finding: there were no unbuilt buildable products left. All 16 shelf products plus the planner already exist as complete deliverables (10 static PDFs + good-days-calendar PDF/ICS on disk; the 3 personalized products render on demand from the block recipe / move-in template; the 7-Day Reset is the email course). The only buildable gap was visual: every product PDF was built but only the planner page showed its pages, so 15 product pages were text-only. Built source/build_product_thumbnails.py (pdftoppm + PIL: cover-portrait 1024x1536, cover-thumb 1500x1500 letterboxed on ivory, plus 3 sample pages at 935x1402 picked at 1/45%/80% of the page count) and rasterised all 10 static products into kua-calculator/public/products/<slug>/. Added a reusable ProductGallery "Look inside" section (cover + two interior pages) to all 10 static pages via an idempotent wiring script. For the 3 personalized products (Compass, Extended, Move-In) whose covers are generated per-buyer, rendered faithful SAMPLE covers with @resvg/resvg-js + the bundled Hanken TTFs (same approach as the chart-image route), matching the PDF cover design exactly (olive M-shield mark, brand eyebrow, hairline rule, clay-italic accent word, KUA line), each clearly labelled "A sample cover", wired via ProductCoverPreview. The Whole-Home Starter Bundle now shows its three real component covers linked to each product. Every product page with a deliverable now shows what the buyer gets; the 7-Day Reset stays text-only (it is emails, so a cover would misrepresent it). Three clean commits, all builds green. NOT done by design: the 2027 editions (#17/#18) - would require fabricating unverified almanac data, which violates the accuracy discipline; deferred to September per the plan. The room-walkthrough videos (#16) need the owner's recordings. Optional future polish flagged for owner review (not done to avoid an un-QA-able layout regression): cover thumbnails on the /products shelf cards. Standing truth: every product is BUILT; the only remaining step per product is the owner's launch action (Stripe Price + env var, plus migrations 0007/0008 + CRON_SECRET for the data-driven and course products), after which I flip launched:true + stripe-live.
COMPASS-CATALOGUE-FULL - 2026-06-13 - The owner recalled "around 28 products"; an Explore pass found the authoritative roster in spec/architect-2026-05-26-stage-5a.md: the 29-SKU Compass catalogue (Stages 5b-5g + 6), of which only the Personal Compass was built. Built the remaining 28 in four batches, all on the existing personalized engine, all launched:false. (1) 22 single-topic Compasses (12 Single-Space + 9 Single-Life-Pillar + Year Ahead, $7/$9): data-driven via lib/compass-catalogue.ts so adding one is a manifest entry + a topic block; recipes/commerce/waitlist built by loops; 22 pages generated by script; 20 new content blocks (welcome-mini, closing-mini, 9 space + 9 pillar) drafted by a 40-agent draft-then-adversarial-voice-check workflow, reusing room-bedroom/desk/dining + year-overlay for 4; a separate "Compass collection" section on /products so the main shelf stays uncluttered. (2) 3 bundles (Nine Life Areas $29, Twelve Spaces $29, Complete Home flagship $49) as pure recipes assembling existing blocks. (3) Couple Compatibility ($19): a two-person form + fulfill-couple action computing both Kua charts + buildSharedRooms (reused from the account compare feature) + a bespoke couple-template; person B stored in product_responses.extras. (4) Pick-Three bundles ($17): a checkbox selection on the post-checkout form + fulfill-pick3 that validates exactly three and assembles a SYNTHETIC recipe at fulfilment time (no static recipe needed). Key engineering learnings: the success-page form now branches on a personalizedForm discriminator ("kua" | "movein" | "couple" | "pick3"); the pick-three synthetic-Product trick lets one action serve any 3-of-N choice without a stored recipe; smoke-blocks.mjs renders all 42 blocks with zero token leaks each build. Voice discipline held: stripped every "honest" from the new content (the owner's de-claim sweep keeps it to ~4 sitewide). All four batches build green and are pushed. The two pieces that needed genuinely new input flows (couple two-person form, pick-three selection UI) were built rather than deferred. Per-product launch is unchanged: owner creates a Stripe Price + sets the env var (STRIPE_PRICE_<SLUG>), then the product flips live. The catalogue products are text-only pages for now (no per-product cover render); that is the one remaining visual polish, flagged for owner review.
CV2-001 - 2026-07-16 - Compass v2 kickoff: architect spec written and the 14-story CV2 queue appended to prd.json. The analysis learning worth keeping: the flagship's biggest gap was not voice or engine but a missing prerequisite step - the phone-compass "find your directions" walkthrough existed only in pillar-wealth.md (a $7 product) while the $14 flagship assumed the reader already knew where East was. When auditing a product family that shares content blocks, grep the cheap SKUs for practical instructions the flagship lacks; block reuse means the best material sometimes lands downstream first.
CV2-002 - 2026-07-16 - The v2 visual system: miniCompassSvg (per-chapter highlighted sector), bedPlacementSvg + deskPlacementSvg (fixed-furniture diagrams with a rotated compass rose so any direction reads as "up"), floorPlanExampleSvg (static worked example), plus .figure/.worksheet/.one-thing CSS in template.ts. The design insight: never rotate the room to show a direction - rotate the rose. The furniture diagram stays identical for all eight directions, so one generator serves every Kua, and the rose alone tells the reader how the picture lands in their own home. Also relaxed the dev sample route's auth gate to non-production only, so sample PDFs can be rendered headlessly for QA. Side discovery recorded as CV2-015-DATA (owner-gated): KUA_MAP rows for Kua 4 and Kua 9 diverge from the classical Da You Nian table; do not fix without owner sign-off because live outputs would change.
CV2-003 - 2026-07-16 - The find-your-directions chapter. Learning: the fastest way to make a directions product feel personal is not more adjectives but a worksheet whose header row is already filled with the reader's own eight directions via the per-quality tokens - the {{shengQiDir}}-style tokens built for the summary table turned out to be exactly the key an interactive worksheet needs. Also: instructional chapters should give the degree dial in one throwaway line (N 0, NE 45...) rather than a table; the table format invites over-precision that a 45-degree sector does not need.
CV2-004 - 2026-07-16 - Sheng Qi + Tian Yi re-authored on the six-section arc and verified in a real render. Two learnings. First, real-home examples in a token-personalised book must name a THIRD person's Kua and direction (Dana, Kua 8, southwest), never the reader's tokens, and those named mappings must be drawn only from Kua rows that are classically uncontested (1,2,3,6,7,8) so the pending CV2-015-DATA fix cannot invalidate a printed example. Second, when a small compass rose carries a needle, the needle tip must stop well short of the highlighted letter (r-44 not r-30) or they collide at print size; captions under 60dpi proofs are the fastest way to catch this class of bug.
CV2-005 - 2026-07-16 - Yan Nian + Fu Wei re-authored. Learning: the six-section template gets stronger when the Avoid-this list does cross-chapter traffic control ("do not give Yan Nian the deadlines - the push seat belongs to Sheng Qi"); the four supportive chapters now form a jobs board where each direction refuses work that belongs to a sibling, which is both classically accurate and the thing that makes the book feel like one system instead of eight essays. Fu Wei's real-home example works by subtraction (ban the laptop from one chair) - a useful counterweight in a genre that always adds objects.
CV2-006 - 2026-07-16 - Huo Hai + Wu Gui re-authored. Learning: cautious chapters land better when the mitigation section leads with an honest RANKING ("Huo Hai is the least costly of the four to land on") - readers stuck with a bad wall need triage instructions, not more caution; and real-home examples for cautious directions should show money NOT being spent (Petra's one-shelf fix) to counter the fear-purchase reflex this genre trains into people.
CV2-007 - 2026-07-16 - Liu Sha + Jue Ming re-authored. Two learnings. First, a real copy bug had shipped in liu-sha.md v1: "for an East group person" hardcoded in a block rendered for both groups - shared personalised blocks need a smoke check for group-specific phrases (east/west group mentions outside identity-* variants), worth adding to smoke-blocks. Second, the renter is the reader most underserved by feng shui books; Sofia's example (freestanding headboard, angled bookcase, movable desk - every move leaves with the lease) gives the priciest-sounding chapter its most practical page.
CV2-008 - 2026-07-16 - The hard-cases FAQ. Learning: the opposite-group couple answer only works when it starts by admitting the arithmetic ("there is no wall that flatters you both") - every softer opening reads as evasion; and each hard case needs to end in exactly one imperative move, because a reader in a hard case is a reader looking for permission to stop optimising.
CV2-009 - 2026-07-16 - At-a-glance card + seven-day log. Learning: a "one page card" is a layout budget, not a copy note - it took three render passes (intro trim, 9pt tables, bagua compact param 180x216, card-scoped CSS) to land everything on one sheet, and the honest way to verify is pdftoppm proofs, never the HTML. The experiment log's tick-one verdict line (Keep / Move back / Try another) matters more than the day rows: it converts a vague week of noticing into a decision.
CV2-010 - 2026-07-16 - Momentum pass on the framing blocks. Learning: stale forward-references are the quiet killer in a product line that ships incrementally - the identity blocks still said a two-person reading was "in the works" and the closing still promised products "when ready" a month after they were built. Any block that mentions ANOTHER product needs re-reading at every launch-state change; grep for "in the works" and "when it ships" across content/ is a cheap pre-release check.
CV2-011 - 2026-07-16 - Recipes, bands, smokes. Measured page counts from real renders: personal 45pp (was 20-26 in v1), extended 56pp, complete home 121pp - the v2 figures and worksheets add roughly 75 percent more pages to the flagship without padding copy, because every added page is a diagram, an example, or a fill-in sheet. Learning: set targetPages from measured renders of BOTH groups (east and west rendered identical 45pp here), never from arithmetic on word counts; figures dominate pagination and words no longer predict pages once a book carries one visual per spread.
CV2-012 - 2026-07-16 - Template polish. Learning: in a paged book assembled from independent blocks, the cheapest unit of visual identity is an h1::before mark - one CSS rule gives every chapter (present and future, across all 29 catalogue products that share the template) the same opener without touching a single content file.
CV2-013 - 2026-07-16 - Sales pages for the three tiers. Learning: page-count claims live in FOUR places here (page metadata description, the What-is-inside list, products.ts targetPages, and lib/product-assets.ts KNOWN_PAGES) - when a band changes, grep for the old numbers across app/, lib/, and components/ or one of the four silently keeps the stale promise. The old pages claimed 25-30 while the recipe targeted 20-26; v2 claims exactly what the render measures.
CV2-014 - 2026-07-16 - Full verification of Compass v2. Renders across Kua 1, 3, 6, 8 (via the female 5-substitution), 9, and a 2016 child birth date all produced 45-page books whose chapter headings match KUA_MAP exactly; extended measured 56pp and complete home 121pp, inside their new bands; npm run build and both smoke scripts green. Learning: pdftotext grep of the chapter H1s ("Your <quality> direction is <compass>") is a two-second per-Kua correctness audit that needs no eyeballs - worth running for every Kua whenever KUA_MAP or the direction blocks change. Standing caveat for the next session: CV2-015-DATA (classical Kua 4 and Kua 9 corrections) remains OPEN and owner-gated; every example person in the new real-home sections was deliberately drawn from uncontested Kua rows (1,2,3,6,7,8) so the pending fix cannot invalidate printed examples.
CV2-015-DATA - 2026-07-17 - Owner approved and applied: Kua 4 and Kua 9 Eight Mansions rows corrected to the classical Da You Nian table across seven files in two projects (server lib, client calculator JS, library chapter 06, both methodology.md copies, sister-site lib + calculator, four smoke-script token sets). Two learnings. First, the provenance chain was visible in a code comment ("Kua 4 and Kua 9 match chapter 6's worked examples verbatim") - the bug was inherited from the library's hedged "approximately this mapping" examples, so a hedge in a source document becomes a hard fact the moment code copies it; audit any "approximately" that feeds a lookup table. Second, batch single-occurrence string replacement of near-identical list lines (- Jue Ming: X) collided between the two examples in methodology.md and silently swapped values; for repeated-shape lines, replace whole blocks or verify by printing the final section, never trust hit counts alone.
LOOK-INSIDE-COMPASS - 2026-07-17 - Personal Compass flipped from "cover" to "full" preview: sample-2.png (the at-a-glance card) and sample-3.png (the Sheng Qi chapter opener with its mini compass) generated at 935x1402 from the corrected Maya/Kua-4 sample render, matching the name and Kua already printed on the existing sample cover. Learning: interior preview pages of a personalised product must be rendered from the SAME sample identity as the cover (Maya, Kua 4), or the preview contradicts itself; and preview regeneration belongs after any data fix, never before - these pages now display the corrected Kua 4 North/Sheng Qi reading. Side find while verifying: myfengshuilove's forked smoke scripts still pointed at kua-calculator's blocks directory (fork artifact); repointed to its own content, both sister smokes green. Owner review copies saved to samples/ (gitignored): Maya Kua 4 and Ana Kua 9, both 45pp.
CV3-001..004 - 2026-07-17 - The advice layer, mined from the feng-shui project's audited advice base (353 entries, AUDIT-002). Three additions to all flagship tiers: a per-Kua element chapter (colors, materials, prosperity dressing; data in lib/kua-element.ts driven by six new tokens), a per-Kua Yan Nian activation recipe inside the relationship chapter (ADV-0162..69 - whose corners match the corrected classical Yan Nian directions, an independent confirmation of CV2-015-DATA), and a before-the-compass form-checks chapter (command position, solid backing, both-sides access, mirrors, arrows and beams). Measured 50/61/126pp; bands and sales pages updated. Two learnings. First, an advice base with per-entry condition keys (kua, kua_group) makes product mining a filter operation, not a reading operation - the 14 kua-conditioned entries out of 353 were findable in one query, and the rest were correctly left to the products that own them (cures, rooms, wealth). Second, when folk-layer material (chimes, ducks, quartz) enters an honest-framing product, the framing must be structural, not a disclaimer: the activation section works because it names what a custom does (keeps a corner deliberately tended) rather than apologising for what it does not.
