# kua-calculator

A free web tool that returns a reader's Kua number, East/West group, and the eight personal directions (four favourable, four unfavourable) with plain-English meanings.

The first product of My Feng Shui Home. Top-of-funnel acquisition channel: someone searches "kua number calculator," lands here, gets the answer in one page, and is offered the deeper methodology, plus an optional free account.

## Stack

- **Next.js** (App Router, TypeScript) on **Vercel**.
- **Supabase** for auth (magic link + Google), Postgres, and Row-Level Security.
- The calculation itself is unchanged vanilla JS, reused from Phases 1-2 as a static island. The four scripts in `public/calculator/` are loaded with `next/script`; no rewrite of the calculator logic.

Phase 3 migrated this project off Flask + Render. The thin Flask server is gone.

## Run locally

```bash
npm install
cp .env.example .env.local   # fill in real Supabase keys for live auth
npm run dev
```

Default URL: http://localhost:3000

For a production-equivalent run:

```bash
npm run build && npm run start
```

The calculator, methodology, and embed pages work fully with the placeholder
values in `.env.local`. Only sign-in / account need a real Supabase project.

## Environment variables

Four variables, documented in `.env.example`:

| Variable | Purpose |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL. |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon key. RLS-protected, safe in the browser. |
| `SUPABASE_SERVICE_ROLE_KEY` | Service-role key. Server only. Bypasses RLS. |
| `NEXT_PUBLIC_SITE_URL` | Canonical site URL, used for auth redirect URLs. |

## Routes

| Path | Purpose |
|---|---|
| `/` | Redirects to `/kua-calculator` (becomes the My Feng Shui Home home page in Stage 4). |
| `/kua-calculator` | Calculator landing page: hero, form, inline result card, explainer, FAQ. |
| `/methodology` | Deep-dive on the Compass School, the Kua formula, and the Eight Mansions. Build-time rendered from `content/methodology.md`. |
| `/embed` | Chrome-free variant for iframe embedding in third-party pages. |
| `/sign-in` | Magic-link and Google sign-in. |
| `/account` | Auth-gated dashboard: email, saved birth data, marketing opt-in, delete account. |
| `/privacy` | Privacy summary. |
| `/auth/callback` | Magic-link / OAuth code exchange. |
| `/auth/sign-out` | Clears the session. |
| `/robots.txt` | AI-bot policy (generated by `app/robots.ts`). |

## Database

The Supabase schema lives in `supabase/migrations/0001_init.sql`: `profiles`
and `saved_charts` tables, Row-Level Security on both, and a `handle_new_user`
trigger that creates a profile row on signup. Apply it in the Supabase SQL
editor (or via the Supabase CLI) when setting up the project.

## Embed it

```html
<iframe
  src="https://your-host/embed"
  width="100%"
  height="900"
  style="border:0;max-width:720px;"
  loading="lazy"
  title="Kua number calculator"
></iframe>
```

`/embed` sends `Content-Security-Policy: frame-ancestors *`; every other route
sends `frame-ancestors 'self'` so the account pages cannot be framed.

## Design

- Palette: warm naturals - cream `#f1e9d8`, sand `#e0d3b8`, deep olive green `#4f5a36`, clay `#be6b43`, deep ink `#2a271e`. Full system in `brand/BRAND_BOOK.md`.
- Typeface: Hanken Grotesk throughout, with Bilbo Swash Caps for the "My Feng Shui Home" signature. Loaded via `next/font/google`.
- No tracking, no analytics on the calculator core. The account layer uses Supabase by design.

## Tests

Open the browser console on `/kua-calculator` and verify:

```js
calculateKua(1978, 'male')   // => 4
calculateKua(1985, 'female') // => 9
```

## Honest framing

The Kua system is a structured decision tool for spatial design choices. It is not a prediction. Copy on the site never promises outcomes.
